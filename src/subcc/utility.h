#pragma once

#include <string>

namespace subcc
{
using namespace std;
string stringOfInt(int);
char charOf(string const &);
int intOfDec(string const &);
int intOfHex(string const &);
} // namespace subcc
